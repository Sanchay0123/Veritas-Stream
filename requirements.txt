# --- Backend & Web Framework ---
django>=5.0
django-environ         # For managing .env and 1TB paths
djangorestframework    # For the API layer

# --- Cybersecurity & Forensics ---
cryptography>=42.0.0   # For RSA signatures
pycryptodome           # Support for advanced crypto ops
python-dotenv          # Environment variables
python-magic


# --- AI & Image Processing ---
opencv-python          # Core image/video processing
numpy                  # Math for pixel arrays
# Run the specific pip command below for these:
opencv-python-headless
# torch (installed via: pip install torch --index-url https://download.pytorch.org/whl/cu121)
# For the RTX 3050, you'll install torch via a specific URL usually, 

# but we list it here for the environment check:
torch
torchvision
torchaudio

# --- Utilities ---
requests               # External API calls
python-magic           # For file type verification (MIME)
